spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:${DB_PORT}/${DB_DATABASE_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect
    defer-datasource-initialization: true
    sql:
      init:
        mode: always
        encoding: UTF-8

  jwt:
    secret:
      key: ${JWT_SECRET_KEY}

cloud:
  aws:
    s3:
      bucket: ddarelro
    credentials:
      access-key: AKIAVNRLFCIUCT6TY2UE
      secret-key: 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
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false